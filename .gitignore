/vendor/
/owa-data/
owa-config.php
node_modules/
modules/base/dist/
# Web-facing built asset tree -- emitted by webpack (reporting JS + combined CSS +
# their copied url() deps). Pure build output, regenerated by `npm run build`; shipped
# in the release tarball, never committed. See webpack.config.js / build.manifest.json.
public/
/.phpunit.cache/
/test-results/
/playwright-report/
