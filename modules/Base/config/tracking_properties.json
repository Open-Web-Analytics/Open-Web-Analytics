{
    "REMOTE_HOST": {
        "set_by": "request",
        "from": [
            "REMOTE_HOST"
        ],
        "column": "remote_host",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "filter": true,
        "callbacks": [
            "owa_trackingEventHelpers::remoteHostDefault"
        ],
        "note": "The visitor’s NETWORK host, reverse DNS of their address — not the page’s host and not this server’s. v1 reduced it to a registered domain through the Public Suffix List and reported it as the `host` dimension (\"the visitor came from some host\"); v2 gave that name to the page’s host, so this needs a column of its own. Usually empty in practice: Apache only fills REMOTE_HOST with HostnameLookups On, which is off by default."
    },
    "HTTP_USER_AGENT": {
        "set_by": "request",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "raw_ua",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "filter": true,
        "callbacks": [
            "owa_trackingEventHelpers::userAgentDefault"
        ]
    },
    "language": {
        "set_by": "request",
        "from": [
            "HTTP_ACCEPT_LANGUAGE"
        ],
        "column": "language",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "filter": true,
        "callbacks": [
            "owa_trackingEventHelpers::languageDefault"
        ]
    },
    "ip_address": {
        "set_by": "request",
        "from": [
            "HTTP_CLIENT_IP",
            "HTTP_X_FORWARDED_FOR",
            "HTTP_X_CLUSTER_CLIENT_IP",
            "HTTP_FORWARDED_FOR",
            "REMOTE_ADDR"
        ],
        "column": "ip_address",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "filter": true,
        "callbacks": [
            "owa_trackingEventHelpers::ipAddressDefault"
        ]
    },
    "ts": {
        "set_by": "request",
        "from": [
            "clock"
        ],
        "column": "ts",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": true,
        "filter": false,
        "callbacks": [
            "owa_trackingEventHelpers::edgeTimestampMicroseconds"
        ],
        "note": "Edge receipt in microseconds. owa_event_raw.ts, and an input to the derived event id -- the resolution is what keeps two events of one session apart. Environmental so a request cannot set it and a queue drain cannot restamp it."
    },
    "event_type": {
        "set_by": "client",
        "from": [
            "event_type"
        ],
        "column": "event_type",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "What happened. The tracker sends it and log.php also reads it with getRequestParam to call setEventType(), which sets a MEMBER -- getEventType() prefers that member and falls back to this property. Declared required:false with no default so the two cannot disagree: a default here would fabricate an event_type on events that carry their type on the member instead. It is already a column on the queue table, and v2 logs it on the fact row."
    },
    "page_location": {
        "set_by": "client",
        "from": [
            "page_location"
        ],
        "column": "page_location",
        "events": [
            "*"
        ],
        "data_type": "url",
        "required": false,
        "callbacks": [],
        "note": "The complete page URL, query included, with nothing stripped. owa_event_raw.page_location, and the evidence the campaign tags are parsed out of -- page_url is the CANONICAL form and cannot answer that a second time. No callbacks on purpose: a filter here would defeat the column. A tracker cached before this shipped sends only page_url; Classes\\Beacon\\Compat renames it."
    },
    "page_title": {
        "set_by": "client",
        "from": [
            "page_title"
        ],
        "column": "page_title",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::utfEncodeProperty"
        ]
    },
    "fsts": {
        "set_by": "client",
        "from": [
            "fsts"
        ],
        "column": "visitor_fsts",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": []
    },
    "psts": {
        "set_by": "client",
        "from": [
            "psts"
        ],
        "column": "prior_session_start_ts",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": []
    },
    "sts": {
        "set_by": "client",
        "from": [
            "sts"
        ],
        "column": "session_start_ts",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": []
    },
    "beacon_version": {
        "set_by": "client",
        "from": [
            "beacon_version"
        ],
        "column": "beacon_version",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "The tracker's BEACON FORMAT generation, sent on every beacon and stored on the row. Not a contract and not consulted at ingest: it is the evidence for deciding when a compat bridge can be deleted, which is otherwise a guess about how long a customer's cache policy lets an old tracker live. NULL means a tracker from before versioning -- generation 0 -- which is every tracker in the wild at the time this shipped."
    },
    "event_seq": {
        "set_by": "client",
        "from": [
            "event_seq"
        ],
        "column": "event_seq",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "REQUEST scoped: the event's position in its session, counted on the device and stamped when the event is built. The cube sorts a session on this rather than on ts, because ts is edge receipt and a late beacon would otherwise sort after events that happened after it. Not required -- a tracker cached from before this sends nothing and the row keeps NULL, which the sort reads as 'no device order' rather than as position zero."
    },
    "num_prior_sessions": {
        "set_by": "client",
        "from": [
            "nps"
        ],
        "column": "prior_sessions",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": true,
        "callbacks": []
    },
    "is_new_session_start": {
        "set_by": "client",
        "from": [
            "is_new_session_start"
        ],
        "events": [
            "*"
        ],
        "data_type": "boolean",
        "required": true,
        "default_value": false,
        "callbacks": [],
        "note": "REQUEST scoped: rides exactly the one beacon that created the session, and is what SessionHandlers branches create-vs-update on."
    },
    "is_new_visitor_created": {
        "set_by": "client",
        "from": [
            "is_new_visitor_created"
        ],
        "events": [
            "*"
        ],
        "data_type": "boolean",
        "required": true,
        "default_value": false,
        "callbacks": [],
        "note": "REQUEST scoped: marks the one request that minted the visitor. Nothing consumes it yet -- it exists so v2 can raise first_visit from the request that actually created the visitor. Do not drop it as unused."
    },
    "user_name": {
        "set_by": "client",
        "from": [
            "user_name"
        ],
        "param": "user_name",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::setUserName"
        ],
        "note": "A display name the SITE supplies, through OWA.setUserName(), which writes it to the visitor store so it rides every later beacon. Not user_id, which is the site’s own identifier for the person and has a column. IT WAS DECLARED set_by request, from the authenticated OWA session, and that made the tracker’s own setter dead: an environmental property is server-owned, so admitRequestParams() refused the value the beacon carried and the callback then filled it from whoever was logged into OWA — on a tracked site, nobody. Client-set, so the wire value is admitted. The callback stays and still gates on log_visitor_pii, which now gates the SUPPLIED value too, and still falls back to the OWA user when log_owa_user_names is on. A param, not a column: PII nothing groups by, and params is where a value with no dimension belongs."
    },
    "user_email": {
        "set_by": "client",
        "from": [
            "user_email"
        ],
        "param": "user_email",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::setEmailAddress"
        ],
        "note": "As user_name: client-supplied, PII-gated, and a param rather than a column. No tracker method sets it today — a site passes it through globalEventProperties — and it is client-settable so that such a value is admitted rather than silently refused. log_visitor_pii gates it, and log_owa_user_names still backfills the logged-in OWA user’s address. An older tracker sends it as email_address; Classes\\Beacon\\Compat renames it."
    },
    "HTTP_REFERER": {
        "set_by": "client",
        "from": [
            "HTTP_REFERER"
        ],
        "column": "referer_url",
        "events": [
            "*"
        ],
        "data_type": "url",
        "required": false,
        "callbacks": []
    },
    "target_url": {
        "set_by": "client",
        "from": [
            "target_url"
        ],
        "column": "target_url",
        "events": [
            "click",
            "file_download"
        ],
        "data_type": "url",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::makeUrlCanonical"
        ]
    },
    "tagged_source": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "tagged_source",
        "events": [
            "page_view",
            "session_start",
            "first_visit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveTaggedSource"
        ],
        "note": "What the landing URL was tagged with. A claim, not a conclusion, and one the SERVER reads: the tags are parsed out of landing_url for the Property’s own campaign keys, so a beacon cannot assert its own attribution. Neither wire contract has ever carried a tagged_* field. The answer over the claim — the tag if there was one, else the referrer classified — is the cube pass’s, on the cube row."
    },
    "tagged_medium": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "tagged_medium",
        "events": [
            "page_view",
            "session_start",
            "first_visit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveTaggedMedium"
        ],
        "note": "What the landing URL was tagged with. A claim, not a conclusion, and one the SERVER reads: the tags are parsed out of landing_url for the Property’s own campaign keys, so a beacon cannot assert its own attribution. Neither wire contract has ever carried a tagged_* field. The answer over the claim — the tag if there was one, else the referrer classified — is the cube pass’s, on the cube row."
    },
    "tagged_campaign": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "tagged_campaign",
        "events": [
            "page_view",
            "session_start",
            "first_visit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveTaggedCampaign"
        ],
        "note": "What the landing URL was tagged with. A claim, not a conclusion, and one the SERVER reads: the tags are parsed out of landing_url for the Property’s own campaign keys, so a beacon cannot assert its own attribution. Neither wire contract has ever carried a tagged_* field. The answer over the claim — the tag if there was one, else the referrer classified — is the cube pass’s, on the cube row."
    },
    "tagged_ad": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "tagged_ad",
        "events": [
            "page_view",
            "session_start",
            "first_visit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveTaggedAd"
        ],
        "note": "What the landing URL was tagged with. A claim, not a conclusion, and one the SERVER reads: the tags are parsed out of landing_url for the Property’s own campaign keys, so a beacon cannot assert its own attribution. Neither wire contract has ever carried a tagged_* field. The answer over the claim — the tag if there was one, else the referrer classified — is the cube pass’s, on the cube row."
    },
    "tagged_terms": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "tagged_search_terms",
        "events": [
            "page_view",
            "session_start",
            "first_visit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveTaggedTerms"
        ],
        "note": "What the landing URL was tagged with. A claim, not a conclusion, and one the SERVER reads: the tags are parsed out of landing_url for the Property’s own campaign keys, so a beacon cannot assert its own attribution. Neither wire contract has ever carried a tagged_* field. The answer over the claim — the tag if there was one, else the referrer classified — is the cube pass’s, on the cube row."
    },
    "click_x": {
        "set_by": "client",
        "from": [
            "click_x"
        ],
        "column": "click_x",
        "events": [
            "click"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "click_y": {
        "set_by": "client",
        "from": [
            "click_y"
        ],
        "column": "click_y",
        "events": [
            "click"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "page_width": {
        "set_by": "client",
        "from": [
            "page_width"
        ],
        "column": "page_width",
        "events": [
            "click"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "The viewport at the moment of the CLICK, which is what makes click_x and click_y mean anything -- a coordinate without the frame it was measured in cannot be placed on a heatmap of a different window size. Scoped to click like click_x, click_y and element_path.\nIT READ events: [\"*\"], which claimed every event carries it. Neither beacon contract has ever shown it on anything but a click, so the row builder was reading a property no page_view sends and the column was NULL on every non-click row -- the honest record either way, but the registry was describing a wire that does not exist.\nThe domstream beacon sets both too, and is unaffected: domstream is not a v2 event -- V2Event refuses it, and none of its own fields are in this registry -- so its handler reads the property off the event directly. `events` says which EVENT TYPES carry a property, and is read by paramsForEvent() and propertiesForEvent(); it is not a gate in setTrackerProperties(), which walks the whole map."
    },
    "page_height": {
        "set_by": "client",
        "from": [
            "page_height"
        ],
        "column": "page_height",
        "events": [
            "click"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "The viewport at the moment of the CLICK, which is what makes click_x and click_y mean anything -- a coordinate without the frame it was measured in cannot be placed on a heatmap of a different window size. Scoped to click like click_x, click_y and element_path.\nIT READ events: [\"*\"], which claimed every event carries it. Neither beacon contract has ever shown it on anything but a click, so the row builder was reading a property no page_view sends and the column was NULL on every non-click row -- the honest record either way, but the registry was describing a wire that does not exist.\nThe domstream beacon sets both too, and is unaffected: domstream is not a v2 event -- V2Event refuses it, and none of its own fields are in this registry -- so its handler reads the property off the event directly. `events` says which EVENT TYPES carry a property, and is read by paramsForEvent() and propertiesForEvent(); it is not a gate in setTrackerProperties(), which walks the whole map."
    },
    "dom_element_class": {
        "set_by": "client",
        "from": [
            "dom_element_class"
        ],
        "param": "element_class",
        "events": [
            "click"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "dom_element_id": {
        "set_by": "client",
        "from": [
            "dom_element_id"
        ],
        "column": "element_id",
        "events": [
            "click"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "dom_element_name": {
        "set_by": "client",
        "from": [
            "dom_element_name"
        ],
        "param": "element_name",
        "events": [
            "click"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "dom_element_tag": {
        "set_by": "client",
        "from": [
            "dom_element_tag"
        ],
        "column": "element_tag",
        "events": [
            "click"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "dom_element_text": {
        "set_by": "client",
        "from": [
            "dom_element_text"
        ],
        "param": "element_text",
        "events": [
            "click"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by dom.click only. Column type from base.click."
    },
    "action_name": {
        "set_by": "client",
        "from": [
            "action_name"
        ],
        "param": "action_name",
        "events": [
            "custom_event"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by track.action only. Column type from base.action_fact."
    },
    "action_label": {
        "set_by": "client",
        "from": [
            "action_label"
        ],
        "param": "action_label",
        "events": [
            "custom_event"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by track.action only. Column type from base.action_fact."
    },
    "action_group": {
        "set_by": "client",
        "from": [
            "action_group"
        ],
        "param": "action_group",
        "events": [
            "custom_event"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by track.action only. Column type from base.action_fact."
    },
    "numeric_value": {
        "set_by": "client",
        "from": [
            "numeric_value"
        ],
        "param": "numeric_value",
        "events": [
            "custom_event"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "Carried by track.action only. Column type from base.action_fact."
    },
    "ct_order_id": {
        "set_by": "client",
        "from": [
            "ct_order_id"
        ],
        "column": "transaction_id",
        "events": [
            "purchase"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by ecommerce.transaction only. Column type from base.commerce_transaction_fact, where the wire name differs from the column: ct_total is total_revenue, ct_country is billing_country."
    },
    "ct_order_source": {
        "set_by": "client",
        "from": [
            "ct_order_source"
        ],
        "param": "order_source",
        "events": [
            "purchase"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by ecommerce.transaction only. Column type from base.commerce_transaction_fact, where the wire name differs from the column: ct_total is total_revenue, ct_country is billing_country."
    },
    "ct_gateway": {
        "set_by": "client",
        "from": [
            "ct_gateway"
        ],
        "param": "gateway",
        "events": [
            "purchase"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Carried by ecommerce.transaction only. Column type from base.commerce_transaction_fact, where the wire name differs from the column: ct_total is total_revenue, ct_country is billing_country."
    },
    "ct_total": {
        "set_by": "client",
        "from": [
            "ct_total"
        ],
        "column": "revenue",
        "events": [
            "purchase"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::toMinorUnits"
        ],
        "note": "Carried by ecommerce.transaction only. Column type from base.commerce_transaction_fact, where the wire name differs from the column: ct_total is total_revenue, ct_country is billing_country. integer is the nearest type this vocabulary has to numeric -- it applies $var + 0, so 12.50 stays 12.5 rather than truncating -- and toMinorUnits() then converts it to what the column stores. The conversion used to happen in the row builder, which is the last place it could: a property whose value is not what its column stores is a property the registry cannot describe."
    },
    "ct_tax": {
        "set_by": "client",
        "from": [
            "ct_tax"
        ],
        "column": "tax",
        "events": [
            "purchase"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::toMinorUnits"
        ],
        "note": "Carried by ecommerce.transaction only. Column type from base.commerce_transaction_fact, where the wire name differs from the column: ct_total is total_revenue, ct_country is billing_country. integer is the nearest type this vocabulary has to numeric -- it applies $var + 0, so 12.50 stays 12.5 rather than truncating -- and toMinorUnits() then converts it to what the column stores. The conversion used to happen in the row builder, which is the last place it could: a property whose value is not what its column stores is a property the registry cannot describe."
    },
    "ct_shipping": {
        "set_by": "client",
        "from": [
            "ct_shipping"
        ],
        "column": "shipping",
        "events": [
            "purchase"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::toMinorUnits"
        ],
        "note": "Carried by ecommerce.transaction only. Column type from base.commerce_transaction_fact, where the wire name differs from the column: ct_total is total_revenue, ct_country is billing_country. integer is the nearest type this vocabulary has to numeric -- it applies $var + 0, so 12.50 stays 12.5 rather than truncating -- and toMinorUnits() then converts it to what the column stores. The conversion used to happen in the row builder, which is the last place it could: a property whose value is not what its column stores is a property the registry cannot describe."
    },
    "ct_line_items": {
        "set_by": "client",
        "from": [
            "ct_line_items"
        ],
        "param": "items",
        "events": [
            "purchase"
        ],
        "data_type": "json",
        "required": false,
        "callbacks": [],
        "note": "Deliberately untyped. It arrives as a JSON string from the beacon but as an ARRAY when a PHP caller sets it directly, and cleanJson calls json_decode, which takes only a string. Declaring json here fataled every programmatic transaction. Enumerated so the wire surface is complete; coercion needs the polymorphism resolved first."
    },
    "site_id": {
        "set_by": "client",
        "from": [
            "site_id"
        ],
        "column": "site_id",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "The tracked site. logEvent refuses an event naming a site this installation does not have. string, not integer: the integer type applies $var + 0, and these are 19-digit BIGINT ids where that risks precision. Nothing needs them arithmetically."
    },
    "session_id": {
        "set_by": "client",
        "from": [
            "session_id"
        ],
        "column": "session_id",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Minted by the tracker. string, not integer: the integer type applies $var + 0, and these are 19-digit BIGINT ids where that risks precision. Nothing needs them arithmetically."
    },
    "visitor_id": {
        "set_by": "client",
        "from": [
            "visitor_id"
        ],
        "column": "visitor_id",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Minted by the tracker and held in the v store. string, not integer: the integer type applies $var + 0, and these are 19-digit BIGINT ids where that risks precision. Nothing needs them arithmetically."
    },
    "engagement_msec": {
        "set_by": "client",
        "from": [
            "engagement_msec"
        ],
        "column": "engagement_msec",
        "events": [
            "user_engagement",
            "page_view"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "Time accrued on the current page since the last report, in milliseconds. A DELTA, not a session total, so losing one beacon costs one increment rather than one page. Summed at read time; the engaged threshold is applied there too and never at collection."
    },
    "content_group": {
        "set_by": "client",
        "from": [
            "content_group"
        ],
        "column": "content_group",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Author-assigned grouping of pages. Page-scoped: set per page, so it cannot leak onto the next one."
    },
    "consent_state": {
        "set_by": "client",
        "from": [
            "consent_state"
        ],
        "column": "consent_state",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "granted or denied, as the page declared it at send. Per event because it can change mid-session; absent stays absent, because no consent recorded is not the same as consent denied."
    },
    "user_id": {
        "set_by": "client",
        "from": [
            "user_id"
        ],
        "column": "user_id",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "The site's OWN id for a logged-in person -- not user_name, which is a display name. The one field that outlives a cookie, and forward-only: events collected before someone identified themselves stay anonymous."
    },
    "currency": {
        "set_by": "client",
        "from": [
            "currency"
        ],
        "column": "currency",
        "events": [
            "purchase"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "ISO 4217, stored beside revenue. Without it a multi-currency store sums minor units of different things."
    },
    "scroll_depth": {
        "set_by": "client",
        "from": [
            "scroll_depth"
        ],
        "column": "scroll_depth",
        "events": [
            "scroll"
        ],
        "data_type": "integer",
        "required": false,
        "callbacks": [],
        "note": "Percentage of the document reached, on a scroll event. One event per threshold per page."
    },
    "element_path": {
        "set_by": "client",
        "from": [
            "element_path"
        ],
        "column": "element_path",
        "events": [
            "click"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "A CSS-ish path to the clicked element. dom_element_id is real on 0.09% of clicks and populated on 94%, so a path is what actually identifies what was clicked."
    },
    "search_term": {
        "set_by": "client",
        "from": [
            "search_term"
        ],
        "param": "search_term",
        "events": [
            "view_search_results"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "Site-search term, on a view_search_results event. Reaches reporting as the params.search_term path."
    },
    "file_name": {
        "set_by": "client",
        "from": [
            "file_name"
        ],
        "param": "file_name",
        "events": [
            "file_download"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "On a file_download event."
    },
    "file_extension": {
        "set_by": "client",
        "from": [
            "file_extension"
        ],
        "param": "file_extension",
        "events": [
            "file_download"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "On a file_download event."
    },
    "form_id": {
        "set_by": "client",
        "from": [
            "form_id"
        ],
        "param": "form_id",
        "events": [
            "form_start",
            "form_submit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "On form_start / form_submit. Both id and name are carried because either may be absent."
    },
    "form_name": {
        "set_by": "client",
        "from": [
            "form_name"
        ],
        "param": "form_name",
        "events": [
            "form_start",
            "form_submit"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [],
        "note": "On form_start / form_submit."
    },
    "yyyymmdd": {
        "set_by": "event",
        "from": [
            "ts"
        ],
        "column": "yyyymmdd",
        "events": [
            "*"
        ],
        "data_type": "integer",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::deriveYyyymmdd"
        ]
    },
    "host": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "host",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::deriveHost"
        ],
        "note": "The PAGE’s host. Not HTTP_HOST, which is this server’s, and not REMOTE_HOST, which is the visitor’s network. Falls back to whatever the property already held; nothing on the wire can set it, since set_by event means admitRequestParams() refuses a beacon that names it."
    },
    "browser_type": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "browser_type",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::resolveBrowserType"
        ]
    },
    "os": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "os",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::resolveOs"
        ]
    },
    "country": {
        "set_by": "event",
        "from": [
            "ip_address"
        ],
        "column": "country",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::resolveCountry"
        ]
    },
    "city": {
        "set_by": "event",
        "from": [
            "ip_address"
        ],
        "column": "city",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::resolveCity"
        ]
    },
    "state": {
        "set_by": "event",
        "from": [
            "ip_address"
        ],
        "column": "region",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::resolveState"
        ]
    },
    "country_code": {
        "set_by": "event",
        "from": [
            "ip_address"
        ],
        "column": "country_code",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": true,
        "callbacks": [
            "owa_trackingEventHelpers::resolveCountryCode"
        ]
    },
    "page_path": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "page_path",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::derivePagePath"
        ],
        "note": "The path, canonicalised: the Profile’s default page and the trailing slash collapse. Set by its own callback off page_location, like every other property -- it used to be cut inside the row builder, which is how the registry came to declare a property nothing produced."
    },
    "page_query": {
        "set_by": "event",
        "from": [
            "page_location"
        ],
        "column": "page_query",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::derivePageQuery"
        ],
        "note": "The query string with the Profile’s dropped parameters removed -- owa_state and the campaign keys are OWA’s own plumbing and have no business in somebody’s page report. The tags stay recoverable: they are parsed out of page_location, which keeps them."
    },
    "referer_host": {
        "set_by": "event",
        "from": [
            "HTTP_REFERER"
        ],
        "column": "referer_host",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::deriveRefererHost"
        ],
        "note": "The referrer’s host, and what the cube pass classifies source and medium from. Left alone beyond the host: it is somebody else’s URL, and canonicalising it against THIS site’s default page would be a category error."
    },
    "referer_query": {
        "set_by": "event",
        "from": [
            "HTTP_REFERER"
        ],
        "column": "referer_query",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::deriveRefererQuery"
        ],
        "note": "The referrer’s query string, unfiltered -- see referer_host."
    },
    "target_host": {
        "set_by": "event",
        "from": [
            "target_url"
        ],
        "column": "target_host",
        "events": [
            "click",
            "file_download"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::deriveTargetHost"
        ],
        "note": "The click target’s host, so an outbound click is a comparison against host rather than a string test. Read after makeUrlCanonical() has filtered target_url, which the scopes settle: target_url is a client property and this is a derived one."
    },
    "browser_version": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "browser_version",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveBrowserVersion"
        ],
        "note": "From the one user-agent parse in the row builder. It used to be read from a property resolved through an argument-less browscap accessor, which memoises the first parse in the process."
    },
    "os_version": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "os_version",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveOsVersion"
        ]
    },
    "device_type": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "device_type",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveDeviceType"
        ],
        "note": "Derived, not read: ua-parser has no such field, and ‘Other’ is its word for no rule matching. The OS family is what tells a desktop from a phone, and NULL is what an unknown gets rather than a guess at desktop."
    },
    "device_brand": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "device_brand",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveDeviceBrand"
        ]
    },
    "device_model": {
        "set_by": "event",
        "from": [
            "HTTP_USER_AGENT"
        ],
        "column": "device_model",
        "events": [
            "*"
        ],
        "data_type": "string",
        "required": false,
        "callbacks": [
            "owa_trackingEventHelpers::resolveDeviceModel"
        ]
    }
}
